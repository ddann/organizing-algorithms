package tests;


import org.junit.Test;

import algorithms.Main;

/**
 * This isn't actually a test class that would test anything.
 *
 * This could be used to test if it would define the inputs before calling Main.main() and would test the class' output.
 * As well as of course the automatic testing. Which would need file writing/reading testing...
 */
public class AlgortihmsMainTest {

	@Test
	public void testMain() {
		Main.main(null);
	}

}
