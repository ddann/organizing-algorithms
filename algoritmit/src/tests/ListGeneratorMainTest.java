package tests;

import org.junit.Test;

import listGenerator.Main;

/**
 * This isn't actually a test class that would test anything.
 *
 * It could test that the list is created allright thoughth file writing/reading testing isn't very comfortable.
 * An actually it still should have pre-defined inputs. So its testing is omitted here.
 * (This could also be used to test the class' output.)
 */
public class ListGeneratorMainTest {

	@Test
	public void testMain() {
		Main.main(null);
	}

}
